/**
 * Main Application Entry - go2apply
 * Inicialização global e escuta do evento de ciclo de vida dos componentes
 */

window.addEventListener('components:ready', () => {
    // Todos os componentes e páginas foram carregados e montados no DOM
    console.info('⚡ go2apply: Todos os componentes foram carregados com sucesso.');
});
